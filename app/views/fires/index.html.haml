%h1 Listing fires
%table
  %thead
    %tr
      %th Started by
      %th Acres
      %th Still burning
      %th Location
      %th{colspan: "3"}
  %tbody
    - @fires.each do |fire|
      %tr
        %td= fire.started_by
        %td= fire.acres
        %td= fire.still_burning
        %td= fire.location_id
        %td= link_to 'Show', fire
        %td= link_to 'Edit', edit_fire_path(fire)
        %td= link_to 'Destroy', fire, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= form_tag fires_path, :method => "get", id: "search-form" do
  = text_field_tag :search, params[:search], placeholder: "Search started_by"
  = submit_tag "Search", :name => nil

/ = form_tag :controller => 'fires', :action => 'search', :method => 'get' do
/   = text_field_tag :search, params[:search], :id => 'search_field'
/   = submit_tag "Search", :name => nil
/   = link_to_function "Clear", "$('search_field').clear()"
    
/= link_to 'New Fire', new_fire_path

/ = form_tag search_fires_path, method: 'get' do
/   = text_area_tag :search
/   = submit_tag "Search"
